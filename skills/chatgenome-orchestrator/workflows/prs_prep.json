{
  "name": "prs_prep",
  "description": "Run build check and harmonization preparation for the active summary-statistics source before PLINK score-file generation.",
  "source_type": "summary_stats",
  "requested_view": "prs_prep",
  "studio": {
    "renderer": "prs_prep"
  },
  "response_kind": "summary_stats_chat",
  "answer_template": "The {workflow_name} workflow was rerun on the active source.\n\n- Workflow: `{workflow_name}`\n- Active file: `{active_file}`\n- Score file ready: {score_file_ready}\n- Kept rows: {kept_rows}\n- Dropped rows: {dropped_rows}\n\nThe PRS preparation state has been refreshed. Review the PRS Prep card, then run `@plink score` when the score file is ready.",
  "requires": [
    "source_stats_path"
  ],
  "produces": [
    "prs_prep_result",
    "score_file_path"
  ],
  "steps": [
    {
      "tool": "prs_prep_tool",
      "bind": "prs_prep_result"
    }
  ],
  "default_view": "sumstats",
  "notes": [
    "The current implementation completes build check and harmonization readiness only.",
    "Score-file generation is intentionally deferred to the next implementation stage."
  ]
}
